{% import dir_sub_theme ~ '/macro/drupalkorea.macro.html.twig' as m %}
<div class="front-page">
    <section class="post-latest grid4">
        <div class="content">
            <div class="a">{{ m.display_post_latest( '자유토론', 'drupal-freetalk', data.post.drupal_freetalk ) }}</div>
            <div class="b">{{ m.display_post_latest( '질문과답변', 'drupal-ask', data.post.drupal_ask ) }}</div>
            <div class="c">{{ m.display_post_latest( '도움요청', 'drupal-help', data.post.drupal_help ) }}</div>
            <div class="d">{{ m.display_post_latest( '드루팔 강좌', 'drupal-tip', data.post.drupal_tip ) }}</div>
        </div>
    </section>
</div>